# Frontend environment. Copy to `.env.local` when running `npm run dev` outside
# Docker Compose.
#
# NOTE: NEXT_PUBLIC_* values are compiled into the browser bundle. Only ever put
# non-secret configuration here — never an API key, token or password.

NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
